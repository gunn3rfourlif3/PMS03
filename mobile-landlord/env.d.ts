// Placeholder — safe to delete.
//
// An attempt to type the EXPO_PUBLIC_* vars used in src/config.ts. It did not
// work: `process.env` here resolves to a narrow `typeof env` type rather than
// NodeJS.ProcessEnv, so augmenting that namespace has no effect. Left as an
// inert comment rather than a misleading declaration.
export {};
